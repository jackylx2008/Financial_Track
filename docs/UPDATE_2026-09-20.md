# 2026-09-20 银行卡类型与月账单归一化更新

## 1. 银行和卡片类型修正

- 交通银行本批 7 份 PDF 均为借记卡交易流水；不再按 16 位账号推断为信用卡，也不再显示未识别卡片。
- 建设银行来源为个人活期账户交易明细，全部标记为借记卡。摘要中的“信用卡还款”表示交易用途，不代表本方账户是信用卡。
- 邮件机构识别改为优先匹配发件人和银行专名，修复工行月账单被招商银行的通用“信用卡/账单/尾号”规则抢先命中的问题。
- 招商银行真实电子月账单新增交易块解析，保存消费、还款、交易日、入账日、商户及卡尾号。

## 2. 工商银行月账单与打印流水比对

- 工行月度信用卡账单逐笔提取交易卡尾号、交易日、入账日、商户、金额、方向及主卡/副卡角色。
- 信用卡打印流水同步保存交易卡尾号；月账单与打印流水按银行、日期、方向、金额、交易卡尾号和商户进行强匹配。
- 商户文字不能完全对应时，仅在其余字段一致且候选唯一时合并；匹配持续执行到结果稳定，重复运行不再产生新合并。
- 合并记录使用 PDF 的精确交易时间，保留月账单完整摘要、主副卡角色、账户尾号及全部邮件/PDF 来源。
- 信用卡还款继续保留邮件账单中的完整还款摘要。

## 3. 审核 HTML

- `bank_transactions_full_review.html` 新增“交易卡尾号”和“主/副卡”列。
- 原“卡号尾号”明确改名为“账户尾号”，用于展示打印流水对应的账户或共享额度卡尾号。
- 银行归一化结果及审核 HTML 删除“交易类型”字段和筛选项。
- 工行借记卡对方账号原样保留（包括中间四位星号）；序号和地区代码仅留在原始行，不再误写到渠道。
- 当前审核集不再出现交通银行信用卡、交通银行未识别卡片、建设银行信用卡或工商银行未识别卡片。

## 4. 当前数据结果

- 原始候选流水 66,576 条，过滤 46 条非交易内容，最终归一化为 39,220 条。
- 工行共享额度跨卡合并 14,371 次；工行月账单与打印流水匹配 12,852 次，其中信用卡还款 32 次。
- 当前分类：工商银行信用卡 14,463 条、工商银行借记卡 4,577 条、交通银行借记卡 519 条、建设银行借记卡 614 条、招商银行信用卡 4 条、光大银行借记卡 1,516 条、支付宝支付账户 11,032 条、微信支付账户 6,495 条。
- 工行信用卡记录中保存主卡角色 8,529 条、副卡角色 4,190 条；其余仅有打印流水来源的记录暂无月账单角色。
- 7 份交通银行 PDF 共 44 页按需调用本地 AI/OCR，全部成功，无未解决附件。
- 银行及支付账户流水和统一财务事实均为 39,220 条，完整指纹、来源哈希及修正前后版本关系继续保留。
- 补回 12,600 个旧版共享卡中间版本，使更早的单卡记录、共享账户版本与当前月账单合并版本形成连续历史链；其中 199 个已不再由当前原始解析结果产生的旧版本作为历史终点保留。

## 5. 光大银行借记卡流水

- 银行归一化流程自动发现 `raw_data/中国光大银行账户明细查询清单.xls`。
- 支出金额和存入金额分别归一化为支出和收入，合并交易日期与交易时间。
- 保存余额、摘要、对方户名和对方账号原文；星号掩码不做删改，并保留源工作表、行号和文件 SHA-256。
- 光大银行记录固定标记为借记卡，并进入完整审核 HTML 和统一财务事实层。

## 6. 支付宝和微信支付流水

- 自动发现 `raw_data/taobao/csv/*.csv` 与 `raw_data/weixin/csv/*.xlsx`；目录名称虽然为 `csv`，微信原始文件实际是 XLSX。
- 支付宝提取 11,032 条，微信支付提取 6,495 条，统一标记为支付账户。
- 保存交易时间、收支方向、金额、交易对方、商品全名、支付方式、状态、交易号及原始行；淘宝来源同时保存平台标识。
- 支付宝“退款成功”按收入处理；部分退款使用实际成功退款金额，早期导出把全额退款写为 0.00 时回退使用原订单金额。
- 普通“不计收支”内部划转和微信中性交易不进入归一化流水。
- 所有来源文件保存完整 SHA-256，CSV 行号或 XLSX 工作表与行号继续用于来源定位。

## 7. 拼多多、美团和京东购物订单

- 订单归一化自动发现 `raw_data/pdd`、`raw_data/meituan` 和 `raw_data/jd`下的截图、PDF 与表格文件。
- 拼多多和美团截图识别结果按原图名缓存到 `raw_data/order_json/<platform>`，后续运行只识别缺失 JSON 的截图；GUI 可选择强制重新识别。
- 京东 PDF 优先读取文本层，仅对字体编码异常页面调用本地 AI/OCR；保存 PDF 文件、页码和订单在页内的序号。
- 平台目录下 CSV、XLS 与 XLSX 按常见中文列名自动映射，保留工作表和行号。
- 京东“已拆分”父订单在订单审核层保留，但不重复生成消费事实；实际商品子订单继续进入财务事实及付款关联。
- 新增 `orders_full_review.html`，支持平台、日期、金额区间、商户、商品全名和状态筛选，来源 PDF 或截图可用本机默认程序打开。
- 订单记录与银行流水一样保存来源文件完整 SHA-256、归一化指纹、版本号和修正前后关系。
- 本轮实际读取拼多多截图 147 张、美团截图 292 张、京东 PDF 45 份共 152 页；截图识别均无失败，京东仅 3 个无文本层页面调用本地 AI/OCR。
- 共形成 2,590 条页面级候选，合并 210 条重叠记录后得到 2,380 条审核记录：拼多多 438 条、美团 1,155 条、京东 787 条。
- 京东 84 条“已拆分”父订单仅保留在审核层；最终新增 2,296 条购物消费事实，并与银行及支付流水建立 2,126 条关联，其中强关联 1,719 条、候选关联 407 条。
- 2,380 条订单均生成完整归一化指纹且来源哈希缺失为 0。拼多多截图页面本身不显示订单日期，相关记录不根据截图文件名猜测日期，需在审核页结合来源人工确认。
