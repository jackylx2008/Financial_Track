# 2026-09-23 更新记录

## 工商银行信用卡 PDF 信息源人工选择

- GUI“邮件获取账单”页新增“选择工行信用卡 PDF 来源”按钮。它读取既有邮件附件提取清单，不重新扫描
  邮箱，不打开 PDF 做 OCR，也不调用本地 AI。
- 审核页以原始邮件为单位列出工行信用卡流水的全部分卷，提供免密 PDF 和原始 EML 的本机打开链接；
  工行借记卡 PDF 不进入候选清单。
- 人工核对后只能选定一组邮件及其全部 PDF 作为账单信息来源。保存请求由仅监听 `127.0.0.1` 且带随机
  会话令牌的本地审核服务接收，并再次校验所选标识必须属于当前候选集合。
- 选择结果写入 `bank_transaction_review.source_selection_file` 指定的
  `raw_data/bank_source_selection.local.txt`。保存时保留与本次候选无关的既有排除项，同时将未选中的
  工行信用卡邮件组登记为排除来源；下一次银行归一化会在解析前跳过这些来源。
- 审核页输出为 `processed_data/normalized/icbc_credit_pdf_source_review.html`。审核页、原始资料、完整
  SHA-256 和人工选择文件均在 Git 忽略目录内，不提交个人财务信息。

## 本机数据验证

- 当前邮件附件清单识别到 2 组工行信用卡邮件、共 12 个免密分卷 PDF；2 个工行借记卡 PDF 未被误列。
- 审核页正确回显既有人工选择，并为每个候选组找到对应原始 EML。

## 工商银行信用卡外币交易修正

- 修正人工审核 PDF 表格的币种列映射：交易金额优先读取“交易币种”，不再因没有通用“币种”列而错误
  回退为人民币。原 PDF 缓存本身内容正确，因此本次没有重新 OCR，也没有调用本地 AI。
- 月度邮件中的实际交易日可能早于 PDF 打印流水的入账日期。跨来源匹配现在同时比较交易日和入账日，
  并把币种纳入匹配键；匹配后保留邮件提供的实际交易日及入账日。
- 修复长英文商户名称导致月度账单行被漏掉的问题，并明确使用“交易金额/交易币种”，不把外币交易的
  入账折算金额误当作原币金额。
- PDF 将不能直接表示的币种写为“其它”时，只允许与同一卡片、方向、原币金额、商户和入账日期均唯一
  对应的邮件账单合并，并采用邮件中的具体 ISO 币种。新增 `HKD`/港币和 `TWD`/新台币中文显示。
- 重建后 `JR CENTRAL` 的 20,900、29,900 和 5,170 三笔均只保留一条日元流水；不存在 5,170 元人民币
  流水。`APPLE ASIA LLC,TAIWAN BRA` 的 800 元“其它币种”流水修正为 800 新台币，并合并邮件与 PDF 来源。
- 本轮银行审核清单由 21,620 条调整为 21,560 条；本地 AI/OCR 尝试次数为 0，所有数据来自既有人工审核
  PDF 缓存和邮件正文。
