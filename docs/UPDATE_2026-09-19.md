# 2026-09-19 更新说明

## 1. 流水完整追溯

- 原始邮件、原始加密附件、实际解析文件和邮件正文均保存完整 SHA-256。
- 银行流水和统一财务事实增加 64 位 `record_fingerprint_sha256` 及稳定的来源谱系指纹。
- `record_version`、`supersedes_record_ids` 和 `supersedes_record_fingerprints_sha256` 关联修正前后的版本。
- 被替代记录分别追加到 `bank_transactions_history.jsonl` 和 `financial_transactions_history.jsonl`，避免重跑覆盖后丢失旧版本。
- 原有邮件 UID、PDF 页码、Excel/CSV 行号和工作表定位字段继续保留。

## 2. 银行字段统一

- 工商银行借记卡 PDF 将对方户名、对方账号、渠道和交易类型拆分保存。
- 工商银行信用卡优先使用账单中的商户名称，并继续区分借贷方向、交易金额和账户余额。
- 建设银行、交通银行及通用 Excel/CSV 解析器兼容常见的对方户名、对方账号、交易类型和渠道列名。
- 本地 AI/OCR 提示和结果模型同步增加对方账号、交易类型与渠道字段。

## 3. 完整审核 HTML

- 主表统一为“商户/对方全名”，并增加“对方账号”和“交易类型”列。
- 对方账号和交易类型均可筛选，原有机构、日期、方向、金额、币种、摘要、渠道和来源筛选继续保留。
- 来源定位展开项提供原始 PDF、Excel、CSV 或邮件文件链接。
- 通过 GUI 打开审核页时，来源文件由本机默认程序打开。

## 4. 本机文件打开安全边界

- GUI 启动仅监听 `127.0.0.1` 的临时审核服务，不开放局域网访问。
- 每次打开审核页使用随机会话令牌。
- 仅允许打开项目 `raw_data/` 下实际存在的文件，拒绝目录外路径。
- GUI 关闭时同步停止临时审核服务。

## 5. 当前数据与验证

- 已重新生成 `bank_transactions_full_review.html`，包含 47,362 条交易和 47,488 个有效来源链接。
- 工商银行借记卡 4,577 条记录均已统一显示对方名称、渠道和交易类型；原文存在对方账号的记录同步显示账号。
- `python -m unittest discover -s tests -v`：86 项测试通过。
- `python -m compileall`、`python -m flake8 .`、HTML JavaScript 语法检查及 `git diff --check` 均通过。
