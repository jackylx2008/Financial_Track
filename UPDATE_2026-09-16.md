# Financial Track 更新说明（2026-09-16）

## 更新范围

本次更新根据 GUI 实际运行反馈，完善了外部本地 AI 服务状态展示、对话连通性验证和日志目录管理。

## GUI 本地 AI 状态

- GUI 启动后在后台检查 `GET /health` 和 `GET /v1/models`，不会阻塞 Tk 主线程。
- 底部状态栏显示服务是否启动、健康状态、配置模型、实际加载模型和本机接口。
- 默认每 30 秒刷新健康与模型状态，也可以点击“测试 AI 连接”手动验证。
- GUI 启动及手动测试时向 `POST /v1/chat/completions` 发送“你好”。
- 发送内容、模型回复或鉴权错误会写入 GUI 的共享日志窗口。
- 模型列表会去重；配置别名与实际模型名不一致时，两者分别显示。

本次实测结果：

```text
健康状态：status=ok
配置模型：local-model
实际加载模型：Qwen3.8-27B-Q4_K_M
对话测试：成功
```

API Key 只从进程环境或被 Git 忽略的 `common.env` 读取，不会显示在 GUI、日志或提交内容中。

## 配置项

`config.yaml` 的 `llamacpp` 段新增：

```yaml
status_timeout_sec: ${LLAMACPP_STATUS_TIMEOUT_SEC:-3}
status_refresh_sec: ${LLAMACPP_STATUS_REFRESH_SEC:-30}
```

对应的 `common.env.example` 使用脱敏示例说明本机服务地址、模型、API Key 和状态刷新配置。

MacBook 远程访问 Windows AI 的预留功能已按要求从 GUI、代码、配置、示例和说明文档中移除；该能力不属于本项目职责。

## 日志目录

- `logging_config.py` 默认日志目录已经是项目根目录的 `logs/`。
- 所有工作流继续使用 `logs/<入口名>.log`。
- 根目录旧 `log/` 已删除，规范目录 `logs/` 保留。
- `logs/` 和 `common.env` 均由 `.gitignore` 排除，不提交本机日志或秘密值。

## 验证结果

完成以下检查：

```text
单元测试：39 项通过
compileall：通过
flake8：通过
git diff --check：通过
GUI 实际启动与“你好”对话日志检查：通过
```

## 使用方式

从项目根目录启动：

```powershell
python main.py
```

如果状态栏提示 `HTTP 401`，应检查本机 `common.env` 中的 `LLAMACPP_API_KEY` 是否与已启动的 AI 服务一致，然后点击“测试 AI 连接”重新验证。
